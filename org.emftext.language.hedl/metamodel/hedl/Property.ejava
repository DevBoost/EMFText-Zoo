epackage hedl;

eclass Property {
	
	getTypeClassname() {
		String mainType = getType().getJavaClassname();
		if (isToMultiplicity()) {
			return "List<" + mainType + ">";
		} else {
			return mainType;
		}
	}
}