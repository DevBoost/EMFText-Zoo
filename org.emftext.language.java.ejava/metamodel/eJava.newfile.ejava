epackage nameSpaceURI;

// imports go here
import org.emftext.language.java.util.UniqueEList;
import org.eclipse.emf.common.util.EList;

eclass MetaClassToExtend {
	
	nameOfDerivedMethod() {
		// custom code goes here and will be merged during the
		// EMF code generation
		EList<EObject> result = new UniqueEList<EObject>();
		// add some code here that fills the derived list
		return result;
	}
}